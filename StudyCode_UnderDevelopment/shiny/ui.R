# Generated by OmopViewer 0.2.0
# Be careful editing this file

ui <- bslib::page_navbar(
  theme = bslib::bs_theme(
    bootswatch = "flatly",   
    primary = "#c8d2c1",     
    secondary = "#dcd6e1",
    success = "#a28ea0",     
    base_font = bslib::font_google("Poppins", wght = 400),
    heading_font = bslib::font_google("Montserrat", wght = 500),
    code_font = bslib::font_google("Fira Code"),
    "font-size-base" = "0.9rem",
  ),
  tags$head(
    tags$style(HTML("
      * { font-size: 0.9rem !important; }
      .navbar { min-height: 60px !important; padding: 0.2rem 0 !important; }
      .navbar { min-height: 60px !important; padding: 0.5rem 0 !important; }
      .accordion-button:not(.collapsed) { background-color: #e3d4f5 !important; color: #000 !important; }
      .background-card-body { font-size: 0.9rem !important; line-height: 1.6 !important; }
      .background-card-body h1 { font-size: 1.6rem !important; }
      .background-card-body h2 { font-size: 1.4rem !important; }
      .background-card-body h3 { font-size: 1.2rem !important; }
      .background-card-body h4 { font-size: 1.1rem !important; }
    "))
  ),
  # theme = bslib::bs_theme(
  #   bootswatch = "flatly",   
  #   primary =  "#c8d2c1",     
  #   secondary = "#dcd6e1",
  #   success = "#a28ea0",     
  #   base_font = font_google("Poppins"),
  #   heading_font = font_google("Montserrat"),
  #   code_font = font_google("Fira Code"),
  # ),
  # tags$head(
  #   tags$style(HTML("
  #   .accordion-button:not(.collapsed) {
  #     background-color: #e3d4f5 !important; /* Lila suave */
  #     color: #000 !important;
  #   }
  # "))
  # ),
  ## SUMARY AND CDM SNAPSHOT ###############################################
  bslib::nav_menu(
    title = "Summary",
    icon = shiny::icon("info"),
    
  bslib::nav_panel(
    title = "Background",
    icon = shiny::icon("disease"),
    div(
      style = "max-width: 1200px; margin: 0 auto;",  # ancho fijo + centrado
      backgroundCard("background.md")   #summary text
    )
  ),
  bslib::nav_panel(
    title = "CDM Snapshot",
    icon = shiny::icon("clipboard-list"),
    #Left side bar
    bslib::layout_sidebar(
      sidebar = bslib::sidebar(
        bslib::accordion(
          bslib::accordion_panel(
            title = "Settings",
            shinyWidgets::pickerInput(
              inputId = "summarise_omop_snapshot_grouping_cdm_name",
              label = "Cdm name",
              choices = filterValues$summarise_omop_snapshot_grouping_cdm_name,
              selected = filterValues$summarise_omop_snapshot_grouping_cdm_name,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3")
            )
          )
        )
      ),
      # Main table visualization
      bslib::navset_tab(
        bslib::nav_panel(
          title = "Snapshot table",
          bslib::card(
            full_screen = TRUE,
            bslib::card_header(
              bslib::popover(
                shiny::icon("download"),
                shinyWidgets::pickerInput(
                  inputId = "summarise_omop_snapshot_gt_17_download_type",
                  label = "File type",
                  selected = "docx",
                  choices = c("docx", "png", "pdf", "html"),
                  multiple = FALSE
                ),
                shiny::downloadButton(outputId = "summarise_omop_snapshot_gt_17_download", label = "Download")
              ),
              class = "text-end"
            ),
            div(
              style = "flex-grow: 1; overflow: auto;",
              gt::gt_output("summarise_omop_snapshot_gt_17")
            )
          )
        )
      )
    )
  )
  ) ,
  ## COHORT COUNTS AND ATTRITION ###############################################
  bslib::nav_menu(
    title = "Cohort counts",
    icon = shiny::icon("circle-user"),
  bslib::nav_panel(
    title = "Cohort count",
    icon = shiny::icon("users"),
    bslib::layout_sidebar(
      sidebar = bslib::sidebar(
        bslib::accordion(
          bslib::accordion_panel(
            title = "Settings",
            shinyWidgets::pickerInput(
              inputId = "summarise_cohort_count_grouping_cdm_name",
              label = "Cdm name",
              choices = filterValues$summarise_cohort_count_grouping_cdm_name,
              selected = filterValues$summarise_cohort_count_grouping_cdm_name,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_cohort_count_settings_table_name",
              label = "Table name",
              choices = filterValues$summarise_cohort_count_settings_table_name[filterValues$summarise_cohort_count_settings_table_name != "denominator"],
              selected = filterValues$summarise_cohort_count_settings_table_name[1],
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_cohort_count_cancer_group",
              label = "Cancer group",
              choices = filterValues$cancer_group_names,
              selected = filterValues$cancer_group_names[1],
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_cohort_count_grouping_cohort_name",
              label = "Cohort name",
              choices = filterValues$summarise_cohort_count_grouping_cohort_name[!grepl("denominator", filterValues$summarise_cohort_count_grouping_cohort_name)],
              selected = filterValues$summarise_cohort_count_grouping_cohort_name,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            )
          ),
          bslib::accordion_panel(
            title = "Variables",
            shinyWidgets::pickerInput(
              inputId = "summarise_cohort_count_variable_name",
              label = "Variable name",
              choices = filterValues$summarise_cohort_count_variable_name,
              selected = filterValues$summarise_cohort_count_variable_name,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3",container = "body")
            )
          )
        )
      ),
      bslib::navset_tab(
        bslib::nav_panel(
          title = "Table cohort count",
          bslib::card(
            full_screen = TRUE,
            bslib::card_header(
              bslib::popover(
                shiny::icon("download"),
                shinyWidgets::pickerInput(
                  inputId = "summarise_cohort_count_gt_9_download_type",
                  label = "File type",
                  selected = "docx",
                  choices = c("docx", "png", "pdf", "html"),
                  multiple = FALSE
                ),
                shiny::downloadButton(outputId = "summarise_cohort_count_gt_9_download", label = "Download")
              ),
              class = "text-end"
            ),
            bslib::layout_sidebar(
              sidebar = bslib::sidebar(
                open = FALSE,
                sortable::bucket_list(
                  header = NULL,
                  sortable::add_rank_list(
                    text = "none",
                    labels = setdiff(filterValues$summarise_cohort_count_tidy_columns, c("variable_name", "table_name", "cdm_name")),
                    input_id = "summarise_cohort_count_gt_9_none"
                  ),
                  sortable::add_rank_list(
                    text = "header",
                    labels = c("variable_name"),
                    input_id = "summarise_cohort_count_gt_9_header"
                  ),
                  sortable::add_rank_list(
                    text = "groupColumn",
                    labels = c("table_name"),
                    input_id = "summarise_cohort_count_gt_9_groupColumn"
                  ),
                  sortable::add_rank_list(
                    text = "hide",
                    labels = c("cdm_name"),
                    input_id = "summarise_cohort_count_gt_9_hide"
                  )
                ),
                position = "right"
              ),
              gt::gt_output("summarise_cohort_count_gt_9")
            )
          )
        ),
        bslib::nav_panel(
          title = "Tidy",
          bslib::card(
            full_screen = TRUE,
            bslib::card_header(
              bslib::popover(
                shiny::icon("download"),
                shiny::downloadButton(outputId = "summarise_cohort_count_tidy_download", label = "Download csv")
              ),
              class = "text-end"
            ),
            div(
              style = "flex-grow: 1; overflow: auto;",
              DT::dataTableOutput("summarise_cohort_count_tidy")
            )
            # DT::dataTableOutput("summarise_cohort_count_tidy")
          )
        ),
        bslib::nav_panel(
          title = "Plot cohort count",
          bslib::card(
            full_screen = TRUE,
            bslib::card_header(
              bslib::popover(
                shiny::icon("download"),
                shiny::numericInput(
                  inputId = "summarise_cohort_count_ggplot2_10_download_width",
                  label = "Width",
                  value = 15
                ),
                shiny::numericInput(
                  inputId = "summarise_cohort_count_ggplot2_10_download_height",
                  label = "Height",
                  value = 10
                ),
                shinyWidgets::pickerInput(
                  inputId = "summarise_cohort_count_ggplot2_10_download_units",
                  label = "Units",
                  selected = "cm",
                  choices = c("px", "cm", "inch"),
                  multiple = FALSE
                ),
                shiny::numericInput(
                  inputId = "summarise_cohort_count_ggplot2_10_download_dpi",
                  label = "dpi",
                  value = 300
                ),
                shiny::downloadButton(outputId = "summarise_cohort_count_ggplot2_10_download", label = "Download")
              ),
              class = "text-end"
            ),
            bslib::layout_sidebar(
              sidebar = bslib::sidebar(
                shinyWidgets::pickerInput(
                  inputId = "summarise_cohort_count_ggplot2_10_facet",
                  label = "facet",
                  selected = "table_name",
                  multiple = TRUE,
                  choices = c("cdm_name", "table_name", "cohort_name"),
                  options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3",container = "body")
                ),
                shinyWidgets::pickerInput(
                  inputId = "summarise_cohort_count_ggplot2_10_colour",
                  label = "colour",
                  selected = "cohort_name",
                  multiple = TRUE,
                  choices = c("cdm_name", "table_name", "cohort_name"),
                  options = list(`actions-box` = TRUE, size = 10, `selected-text-format` = "count > 3",container = "body")
                ),
                position = "right"
              ),
              shiny::plotOutput("summarise_cohort_count_ggplot2_10")
            )
          )
        )
      )
    )
  ),
  bslib::nav_panel(
    title = "Cohort attrition",
    icon = shiny::icon("layer-group"),
    bslib::layout_sidebar(
      sidebar = bslib::sidebar(
        bslib::accordion(
          bslib::accordion_panel(
            title = "Settings",
            shinyWidgets::pickerInput(
              inputId = "summarise_cohort_attrition_grouping_cdm_name",
              label = "Cdm name",
              choices = filterValues$summarise_cohort_attrition_grouping_cdm_name,
              selected = filterValues$summarise_cohort_attrition_grouping_cdm_name,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_cohort_attrition_settings_table_name",
              label = "Table name",
              choices = filterValues$summarise_cohort_attrition_settings_table_name[
                ! filterValues$summarise_cohort_attrition_settings_table_name %in% c("denominator")],
              selected = filterValues$summarise_cohort_attrition_settings_table_name[1],
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 5, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_cohort_attrition_cancer_group",
              label = "Cancer group",
              choices = filterValues$cancer_group_names,
              selected = filterValues$cancer_group_names[1],
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_cohort_attrition_grouping_cohort_name",
              label = "Cohort name",
              choices = filterValues$summarise_cohort_attrition_grouping_cohort_name[!grepl("denominator", filterValues$summarise_cohort_attrition_grouping_cohort_name)],
              selected = filterValues$summarise_cohort_attrition_grouping_cohort_name,
              multiple = TRUE,
              options = list(
                `actions-box` = TRUE,
                size = 5,
                `selected-text-format` = "count > 3",
                `dropdown-align-right` = FALSE,
                container = "body"  # el dropdown sale fuera del sidebar, sin recortes
              )
              # options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3")
            )
          )
        )
      ),
      bslib::navset_tab(
        bslib::nav_panel(
          title = "Tidy",
          bslib::card(
            full_screen = TRUE,
            bslib::card_header(
              bslib::popover(
                shiny::icon("download"),
                shiny::downloadButton(outputId = "summarise_cohort_attrition_tidy_download", label = "Download csv")
              ),
              class = "text-end",
              style = "padding: 3px;"  # Reducir el padding del header
            ),
            div(
              style = "flex-grow: 1; overflow: auto;",
              DT::dataTableOutput("summarise_cohort_attrition_tidy")
            )
            # DT::dataTableOutput("summarise_cohort_attrition_tidy")
          )
        ),
        bslib::nav_panel(
          title = "Diagram",
          bslib::card(
            full_screen = TRUE,
            bslib::card_header(
              bslib::popover(
                shiny::icon("download"),
                shiny::numericInput(
                  inputId = "summarise_cohort_attrition_grViz_4_download_width",
                  label = "Width (px)",
                  value = 15
                ),
                shiny::numericInput(
                  inputId = "summarise_cohort_attrition_grViz_4_download_height",
                  label = "Height (px)",
                  value = 10
                ),
                shiny::downloadButton(outputId = "summarise_cohort_attrition_grViz_4_download", label = "Download")
              ),
              class = "text-end"
            ),
            DiagrammeR::grVizOutput("summarise_cohort_attrition_grViz_4")
          )
        )
      )
    )
  )
  ),
  ## CHARACTERISTICS & LSC ###############################################
  bslib::nav_menu(
    title = "Characteristics & LSC",
    icon = shiny::icon("users-gear"),
  bslib::nav_panel(
    title = "Characteristics: demographics",
    icon = shiny::icon("user"),
    bslib::layout_sidebar(
      sidebar = bslib::sidebar(
        bslib::accordion(
          bslib::accordion_panel(
            title = "Settings",
            shinyWidgets::pickerInput(
              inputId = "summarise_characteristics_demographics_grouping_cdm_name",
              label = "Cdm name",
              choices = filterValues$summarise_characteristics_demographics_grouping_cdm_name,
              selected = filterValues$summarise_characteristics_demographics_grouping_cdm_name,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_characteristics_demographics_cancer_group",
              label = "Cancer group",
              choices = filterValues$cancer_group_names,
              selected = filterValues$cancer_group_names[1],
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_characteristics_demographics_grouping_cohort_name",
              label = "Cohort name",
              choices = filterValues$summarise_characteristics_demographics_grouping_cohort_name,
              selected = filterValues$summarise_characteristics_demographics_grouping_cohort_name,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_characteristics_demographics_grouping_age_gr",
              label = "Age group",
              choices = filterValues$summarise_characteristics_demographics_grouping_age_gr,
              selected = "overall",
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_characteristics_demographics_grouping_sex",
              label = "Sex",
              choices = filterValues$summarise_characteristics_demographics_grouping_sex,
              selected = "overall",
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            )
            # ,
            # shinyWidgets::pickerInput(
            #   inputId = "summarise_characteristics_demographics_grouping_window",
            #   label = "Window",
            #   choices = filterValues$summarise_characteristics_demographics_grouping_window,
            #   selected = filterValues$summarise_characteristics_demographics_grouping_window,
            #   multiple = TRUE,
            #   options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            # )
          ),
          bslib::accordion_panel(
            title = "Variables & Estimates",
            shinyWidgets::pickerInput(
              inputId = "summarise_characteristics_demographics_variable_name",
              label = "Variable name",
              choices = filterValues$summarise_characteristics_demographics_variable_name,
              selected = filterValues$summarise_characteristics_demographics_variable_name,
              multiple = TRUE,
              options = list(
                `actions-box` = TRUE,
                size = 5,
                `selected-text-format` = "count > 3",
                # `live-search` = TRUE,          
                # `live-search-placeholder` = "Search...",
                `dropdown-align-right` = FALSE,
                container = "body"  # el dropdown sale fuera del sidebar, sin recortes
              )
              # options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_characteristics_demographics_estimate_name",
              label = "Estimate name",
              choices = filterValues$summarise_characteristics_demographics_estimate_name,
              selected = filterValues$summarise_characteristics_demographics_estimate_name,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            )
          )
        )
      ),
      bslib::navset_tab(
        bslib::nav_panel(
          title = "Table cohort characteristics",
          bslib::card(
            full_screen = TRUE,
            bslib::card_header(
              bslib::popover(
                shiny::icon("download"),
                shinyWidgets::pickerInput(
                  inputId = "summarise_characteristics_demographics_gt_7_download_type",
                  label = "File type",
                  selected = "docx",
                  choices = c("docx", "png", "pdf", "html"),
                  multiple = FALSE
                ),
                shiny::downloadButton(outputId = "summarise_characteristics_demographics_gt_7_download", label = "Download")
              ),
              class = "text-end"
            ),
            bslib::layout_sidebar(
              sidebar = bslib::sidebar(
                open = FALSE,
                sortable::bucket_list(
                  header = NULL,
                  sortable::add_rank_list(
                    text = "none",
                    labels = setdiff(filterValues$summarise_characteristics_demographics_tidy_columns, c("cohort_name", "cdm_name")),
                    input_id = "summarise_characteristics_demographics_gt_7_none"
                  ),
                  sortable::add_rank_list(
                    text = "header",
                    labels = c("cohort_name"),
                    input_id = "summarise_characteristics_demographics_gt_7_header"
                  ),
                  sortable::add_rank_list(
                    text = "groupColumn",
                    labels = character(),
                    input_id = "summarise_characteristics_demographics_gt_7_groupColumn"
                  ),
                  sortable::add_rank_list(
                    text = "hide",
                    labels =  c("cdm_name"),
                    input_id = "summarise_characteristics_demographics_gt_7_hide"
                  )
                ),
                position = "right"
              ),
              gt::gt_output("summarise_characteristics_demographics_gt_7")
            )
          )
        ),
        bslib::nav_panel(
          title = "Tidy",
          bslib::card(
            full_screen = TRUE,
            bslib::card_header(
              bslib::popover(
                shiny::icon("download"),
                shiny::downloadButton(outputId = "summarise_characteristics_demographics_tidy_download", label = "Download csv")
              ),
              class = "text-end"
            ),
            bslib::layout_sidebar(
              sidebar = bslib::sidebar(
                open = FALSE,
                shinyWidgets::pickerInput(
                  inputId = "summarise_characteristics_demographics_tidy_columns",
                  label = "Columns",
                  choices = filterValues$summarise_characteristics_demographics_tidy_columns,
                  selected = filterValues$summarise_characteristics_demographics_tidy_columns,
                  multiple = TRUE,
                  options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3")
                ),
                shiny::radioButtons(
                  inputId = "summarise_characteristics_demographics_tidy_pivot",
                  label = "Pivot estimates/variables",
                  choices = c("none", "estimates", "estimates and variables"),
                  selected = "none"
                ),
                position = "right"
              ),
              div(
                style = "flex-grow: 1; overflow: auto;",
                DT::dataTableOutput("summarise_characteristics_demographics_tidy")
              )
              # DT::dataTableOutput("summarise_characteristics_demographics_tidy")
            )
          )
        ),
        bslib::nav_panel(
          title = "Plot characteristics",
          bslib::card(
            full_screen = TRUE,
            bslib::card_header(
              bslib::popover(
                shiny::icon("download"),
                shiny::numericInput(
                  inputId = "summarise_characteristics_demographics_ggplot2_8_download_width",
                  label = "Width",
                  value = 15
                ),
                shiny::numericInput(
                  inputId = "summarise_characteristics_demographics_ggplot2_8_download_height",
                  label = "Height",
                  value = 10
                ),
                shinyWidgets::pickerInput(
                  inputId = "summarise_characteristics_demographics_ggplot2_8_download_units",
                  label = "Units",
                  selected = "cm",
                  choices = c("px", "cm", "inch"),
                  multiple = FALSE
                ),
                shiny::numericInput(
                  inputId = "summarise_characteristics_demographics_ggplot2_8_download_dpi",
                  label = "dpi",
                  value = 300
                ),
                shiny::downloadButton(outputId = "summarise_characteristics_demographics_ggplot2_8_download", label = "Download")
              ),
              class = "text-end"
            ),
            bslib::layout_sidebar(
              sidebar = bslib::sidebar(
                shinyWidgets::pickerInput(
                  inputId = "summarise_characteristics_demographics_ggplot2_8_plotStyle",
                  label = "plotStyle",
                  selected = NULL,
                  multiple = FALSE,
                  choices = c("boxplot", "barplot", "scatterplot"),
                  options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
                ),
                shinyWidgets::pickerInput(
                  inputId = "summarise_characteristics_demographics_ggplot2_8_facet",
                  label = "facet",
                  selected = NULL,
                  multiple = TRUE,
                  choices = filterValues$summarise_characteristics_demographics_tidy_columns,
                  options = list(
                    `actions-box` = TRUE, 
                    size = 7, 
                    `selected-text-format` = "count > 3",
                    container = "body"
                  )
                  # options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3")
                ),
                shinyWidgets::pickerInput(
                  inputId = "summarise_characteristics_demographics_ggplot2_8_colour",
                  label = "colour",
                  selected = NULL,
                  multiple = TRUE,
                  choices = filterValues$summarise_characteristics_demographics_tidy_columns,
                  options = list(
                    `actions-box` = TRUE, 
                    size = 7, 
                    `selected-text-format` = "count > 3",
                    container = "body"
                  )
                  # options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3")
                ),
                position = "right"
              ),
              shiny::plotOutput("summarise_characteristics_demographics_ggplot2_8")
            )
          )
        )
      )
    )
  ),
  bslib::nav_panel(
    title = "Characteristics: conditions & drugs",
    icon = shiny::icon("stethoscope"),
    bslib::layout_sidebar(
      sidebar = bslib::sidebar(
        bslib::accordion(
          bslib::accordion_panel(
            title = "Settings",
            shinyWidgets::pickerInput(
              inputId = "summarise_characteristics_clinical_grouping_cdm_name",
              label = "Cdm name",
              choices = filterValues$summarise_characteristics_clinical_grouping_cdm_name,
              selected = filterValues$summarise_characteristics_clinical_grouping_cdm_name,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_characteristics_clinical_cancer_group",
              label = "Cancer group",
              choices = filterValues$cancer_group_names,
              selected = filterValues$cancer_group_names[1],
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_characteristics_clinical_grouping_cohort_name",
              label = "Cohort name",
              choices = filterValues$summarise_characteristics_clinical_grouping_cohort_name,
              selected = filterValues$summarise_characteristics_clinical_grouping_cohort_name,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_characteristics_clinical_grouping_table",
              label = "Characteristics",
              choices = filterValues$summarise_characteristics_clinical_grouping_table[
                filterValues$summarise_characteristics_clinical_grouping_table != c("overall")
              ],
              selected = filterValues$summarise_characteristics_clinical_grouping_table[
                filterValues$summarise_characteristics_clinical_grouping_table != c("overall")
              ],
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_characteristics_clinical_grouping_window",
              label = "Window",
              choices = filterValues$summarise_characteristics_clinical_grouping_window,
              selected = filterValues$summarise_characteristics_clinical_grouping_window,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            )
          ),
          bslib::accordion_panel(
            title = "Strata",
            shinyWidgets::pickerInput(
              inputId = "summarise_characteristics_clinical_grouping_age_gr",
              label = "Age group",
              choices = filterValues$summarise_characteristics_clinical_grouping_age_gr,
              selected = "overall",
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_characteristics_clinical_grouping_sex",
              label = "Sex",
              choices = filterValues$summarise_characteristics_clinical_grouping_sex,
              selected = "overall",
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            )
          )
        )
      ),
      bslib::navset_tab(
        bslib::nav_panel(
          title = "Table cohort characteristics",
          bslib::card(
            full_screen = TRUE,
            bslib::card_header(
              bslib::popover(
                shiny::icon("download"),
                shinyWidgets::pickerInput(
                  inputId = "summarise_characteristics_clinical_gt_7_download_type",
                  label = "File type",
                  selected = "docx",
                  choices = c("docx", "png", "pdf", "html"),
                  multiple = FALSE
                ),
                shiny::downloadButton(outputId = "summarise_characteristics_clinical_gt_7_download", label = "Download")
              ),
              class = "text-end"
            ),
            bslib::layout_sidebar(
              sidebar = bslib::sidebar(
                open = FALSE,
                sortable::bucket_list(
                  header = NULL,
                  sortable::add_rank_list(
                    text = "none",
                    labels = setdiff(filterValues$summarise_characteristics_clinical_tidy_columns, c("cohort_name", "cdm_name", "table")),
                    input_id = "summarise_characteristics_clinical_gt_7_none"
                  ),
                  sortable::add_rank_list(
                    text = "header",
                    labels = c("cohort_name"),
                    input_id = "summarise_characteristics_clinical_gt_7_header"
                  ),
                  sortable::add_rank_list(
                    text = "groupColumn",
                    labels = character(),
                    input_id = "summarise_characteristics_clinical_gt_7_groupColumn"
                  ),
                  sortable::add_rank_list(
                    text = "hide",
                    labels = c("cdm_name" ,"table"),
                    input_id = "summarise_characteristics_clinical_gt_7_hide"
                  )
                ),
                position = "right"
              ),
              gt::gt_output("summarise_characteristics_clinical_gt_7")
            )
          )
        ),
        bslib::nav_panel(
          title = "Tidy",
          bslib::card(
            full_screen = TRUE,
            bslib::card_header(
              bslib::popover(
                shiny::icon("download"),
                shiny::downloadButton(outputId = "summarise_characteristics_clinical_tidy_download", label = "Download csv")
              ),
              class = "text-end"
            ),
            bslib::layout_sidebar(
              sidebar = bslib::sidebar(
                open = FALSE,
                shinyWidgets::pickerInput(
                  inputId = "summarise_characteristics_clinical_tidy_columns",
                  label = "Columns",
                  choices = filterValues$summarise_characteristics_clinical_tidy_columns,
                  selected = filterValues$summarise_characteristics_clinical_tidy_columns,
                  multiple = TRUE,
                  options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3")
                ),
                shiny::radioButtons(
                  inputId = "summarise_characteristics_clinical_tidy_pivot",
                  label = "Pivot estimates/variables",
                  choices = c("none", "estimates", "estimates and variables"),
                  selected = "none"
                ),
                position = "right"
              ),
              div(
                style = "flex-grow: 1; overflow: auto;",
                DT::dataTableOutput("summarise_characteristics_clinical_tidy")
              )
              # DT::dataTableOutput("summarise_characteristics_clinical_tidy")
            )
          )
        )
      )
    )
  ),
  bslib::nav_panel(
    title = "Large Scale Charactertistics - Conditions",
    icon = shiny::icon("chart-bar"),
    bslib::layout_sidebar(
      sidebar = bslib::sidebar(
        bslib::accordion(
          bslib::accordion_panel(
            title = "Settings",
            shinyWidgets::pickerInput(
              inputId = "summarise_large_scale_characteristics_grouping_cdm_name",
              label = "Cdm name",
              choices = filterValues$summarise_large_scale_characteristics_grouping_cdm_name,
              selected = filterValues$summarise_large_scale_characteristics_grouping_cdm_name,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_large_scale_characteristics_cancer_group",
              label = "Cancer group",
              choices = filterValues$cancer_group_names,
              selected = filterValues$cancer_group_names[1],
              multiple = FALSE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_large_scale_characteristics_grouping_cohort_name",
              label = "Cohort name",
              choices = filterValues$summarise_large_scale_characteristics_grouping_cohort_name,
              selected = filterValues$summarise_large_scale_characteristics_grouping_cohort_name,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_large_scale_characteristics_grouping_variable_level",
              label = "Time window",
              choices = filterValues$summarise_large_scale_characteristics_grouping_variable_level,
              selected = filterValues$summarise_large_scale_characteristics_grouping_variable_level,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            )
          ),
          bslib::accordion_panel(
            title = "Estimates",
            shinyWidgets::pickerInput(
              inputId = "summarise_large_scale_characteristics_estimate_name",
              label = "Estimate name",
              choices = filterValues$summarise_large_scale_characteristics_estimate_name,
              selected = filterValues$summarise_large_scale_characteristics_estimate_name,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
          )
        )
      ),
      # bslib::navset_card_tab(
      bslib::navset_tab(  
        bslib::nav_panel(
          title = "Tidy",
          bslib::card(
            full_screen = TRUE,
            bslib::card_header(
              bslib::popover(
                shiny::icon("download"),
                shiny::downloadButton(outputId = "summarise_lsc_conditions_tidy_download", label = "Download csv")
              ),
              class = "text-end"
            ),
            bslib::layout_sidebar(
              sidebar = bslib::sidebar(
                open = FALSE,
                shinyWidgets::pickerInput(
                  inputId = "summarise_large_scale_characteristics_tidy_columns",
                  label = "Columns",
                  choices = filterValues$summarise_large_scale_characteristics_tidy_columns,
                  selected = filterValues$summarise_large_scale_characteristics_tidy_columns,
                  multiple = TRUE,
                  options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
                ),
                position = "right"
              ),
              div(
                style = "flex-grow: 1; overflow: auto;",
                DT::dataTableOutput("summarise_lsc_conditions_tidy")
              )
              # DT::dataTableOutput("summarise_lsc_conditions_tidy")
            )
          )
        )
      )
    )
  ),
  bslib::nav_panel(
    title = "Large Scale Charactertistics - Drugs",
    icon = shiny::icon("capsules"),
    bslib::layout_sidebar(
      sidebar = bslib::sidebar(
        bslib::accordion(
          bslib::accordion_panel(
            title = "Settings",
            shinyWidgets::pickerInput(
              inputId = "summarise_large_scale_characteristics_grouping_cdm_name",
              label = "Cdm name",
              choices = filterValues$summarise_large_scale_characteristics_grouping_cdm_name,
              selected = filterValues$summarise_large_scale_characteristics_grouping_cdm_name,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_large_scale_characteristics_cancer_group",
              label = "Cancer group",
              choices = filterValues$cancer_group_names,
              selected = filterValues$cancer_group_names[1],
              multiple = FALSE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_large_scale_characteristics_grouping_cohort_name",
              label = "Cohort name",
              choices = filterValues$summarise_large_scale_characteristics_grouping_cohort_name,
              selected = filterValues$summarise_large_scale_characteristics_grouping_cohort_name,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
            shinyWidgets::pickerInput(
              inputId = "summarise_large_scale_characteristics_grouping_variable_level",
              label = "Time window",
              choices = filterValues$summarise_large_scale_characteristics_grouping_variable_level,
              selected = filterValues$summarise_large_scale_characteristics_grouping_variable_level,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            )
          ),
          bslib::accordion_panel(
            title = "Estimates",
            shinyWidgets::pickerInput(
              inputId = "summarise_large_scale_characteristics_estimate_name",
              label = "Estimate name",
              choices = filterValues$summarise_large_scale_characteristics_estimate_name,
              selected = filterValues$summarise_large_scale_characteristics_estimate_name,
              multiple = TRUE,
              options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
            ),
          )
        )
      ),
      # bslib::navset_card_tab(
      bslib::navset_tab(  
        bslib::nav_panel(
          title = "Tidy",
          bslib::card(
            full_screen = TRUE,
            bslib::card_header(
              bslib::popover(
                shiny::icon("download"),
                shiny::downloadButton(outputId = "summarise_lsc_conditions_tidy_download", label = "Download csv")
              ),
              class = "text-end"
            ),
            bslib::layout_sidebar(
              sidebar = bslib::sidebar(
                open = FALSE,
                shinyWidgets::pickerInput(
                  inputId = "summarise_large_scale_characteristics_tidy_columns",
                  label = "Columns",
                  choices = filterValues$summarise_large_scale_characteristics_tidy_columns,
                  selected = filterValues$summarise_large_scale_characteristics_tidy_columns,
                  multiple = TRUE,
                  options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
                ),
                # shiny::radioButtons(
                #   inputId = "summarise_lsc_conditions_tidy_pivot",
                #   label = "Pivot estimates/variables",
                #   choices = c("none", "estimates", "estimates and variables"),
                #   selected = "none"
                # ),
                position = "right"
              ),
              div(
                style = "flex-grow: 1; overflow: auto;",
                DT::dataTableOutput("summarise_lsc_drugs_tidy")
              )
              # DT::dataTableOutput("summarise_lsc_drugs_tidy")
            )
          )
        )
      )
    )
  )
  ),
  ## INCIDENCE  ###############################################
  bslib::nav_menu(
    title = "Incidence",
    icon = shiny::icon("shower"),
    bslib::nav_panel(
      title = "Incidence",
      icon = shiny::icon("chart-line"),
      bslib::layout_sidebar(
        sidebar = bslib::sidebar(
          bslib::accordion(
            bslib::accordion_panel(
              title = "Settings",
              shinyWidgets::pickerInput(
                inputId = "incidence_grouping_cdm_name",
                label = "Cdm name",
                choices = filterValues$incidence_grouping_cdm_name,
                selected = filterValues$incidence_grouping_cdm_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_cancer_group",
                label = "Cancer group",
                choices = filterValues$cancer_group_names,
                selected = filterValues$cancer_group_names[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_grouping_outcome_cohort_name",
                label = "Outcome cohort name",
                choices = filterValues$incidence_grouping_outcome_cohort_name,
                selected = filterValues$incidence_grouping_outcome_cohort_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_grouping_analysis_interval",
                label = "Analysis interval",
                choices = filterValues$incidence_grouping_analysis_interval,
                selected = filterValues$incidence_grouping_analysis_interval[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_grouping_incidence_start_date",
                label = "Incidence start date",
                choices = filterValues$incidence_grouping_incidence_start_date,
                selected = filterValues$incidence_grouping_incidence_start_date,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              )
            ),
            bslib::accordion_panel(
              title = "Strata",
              shinyWidgets::pickerInput(
                inputId = "incidence_settings_denominator_sex",
                label = "Denominator sex",
                choices = filterValues$incidence_settings_denominator_sex,
                selected = filterValues$incidence_settings_denominator_sex[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_settings_denominator_age_group",
                label = "Denominator age group",
                choices = filterValues$incidence_settings_denominator_age_group,
                selected = filterValues$incidence_settings_denominator_age_group[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_strata_cols",
                label = "Strata variables",
                choices = filterValues$incidence_strata,
                selected = NULL,  # por defecto ninguna seleccionada = todo overall
                multiple = TRUE,
                options = list(
                  `actions-box` = TRUE,
                  size = 7,
                  # `live-search` = TRUE,
                  container = "body"
                )
              )
            ),
            bslib::accordion_panel(
              title = "Estimates",
              shinyWidgets::pickerInput(
                inputId = "incidence_estimate_name",
                label = "Estimate name",
                choices = filterValues$incidence_estimate_name,
                selected = filterValues$incidence_estimate_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              )
            )
          )
        ),
        bslib::navset_tab(
          bslib::nav_panel(
            title = "Tidy",
            bslib::card(
              full_screen = TRUE,
              bslib::card_header(
                bslib::popover(
                  shiny::icon("download"),
                  shiny::downloadButton(outputId = "incidence_tidy_download", label = "Download csv")
                ),
                class = "text-end"
              ),
              bslib::layout_sidebar(
                sidebar = bslib::sidebar(
                  open = FALSE,
                  shinyWidgets::pickerInput(
                    inputId = "incidence_tidy_columns",
                    label = "Columns",
                    choices = filterValues$incidence_tidy_columns,
                    # selected = filterValues$incidence_tidy_columns,
                    selected = c("denominator_cohort_name", "outcome_cohort_name", 
                                 "incidence_start_date","incidence_end_date", 
                                 "denominator_sex", "denominator_age_group"),
                    multiple = TRUE,
                    options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
                  ),
                  shiny::radioButtons(
                    inputId = "incidence_tidy_pivot",
                    label = "Pivot estimates/variables",
                    choices = c("none", "estimates", "estimates and variables"),
                    selected = "estimates and variables"
                  ),
                  position = "right"
                ),
                div(
                  style = "flex-grow: 1; overflow: auto;",
                  DT::dataTableOutput("incidence_tidy")
                )
                # DT::dataTableOutput("incidence_tidy")
              )
            )
          ),
          bslib::nav_panel(
            title = "Table incidence",
            bslib::card(
              full_screen = TRUE,
              bslib::card_header(
                bslib::popover(
                  shiny::icon("download"),
                  shinyWidgets::pickerInput(
                    inputId = "incidence_gt_18_download_type",
                    label = "File type",
                    selected = "docx",
                    choices = c("docx", "png", "pdf", "html"),
                    multiple = FALSE
                  ),
                  shiny::downloadButton(outputId = "incidence_gt_18_download", label = "Download")
                ),
                class = "text-end"
              ),
              bslib::layout_sidebar(
                sidebar = bslib::sidebar(
                  open=FALSE,
                  sortable::bucket_list(
                    header = NULL,
                    sortable::add_rank_list(
                      text = "none",
                      # labels = c("incidence_start_date", "incidence_end_date", "analysis_complete_database_intervals", "analysis_outcome_washout", "analysis_repeated_events", "denominator_age_group", "denominator_days_prior_observation", "denominator_end_date", "denominator_sex", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk", "variable_name", "variable_level"),
                      labels = setdiff(filterValues$incidence_tidy_columns, c("estimate_name","cdm_name","denominator_cohort_name" )),
                      input_id = "incidence_gt_18_none"
                    ),
                    sortable::add_rank_list(
                      text = "header",
                      labels = "estimate_name",
                      input_id = "incidence_gt_18_header"
                    ),
                    sortable::add_rank_list(
                      text = "groupColumn",
                      labels = NULL,
                      input_id = "incidence_gt_18_groupColumn"
                    ),
                    sortable::add_rank_list(
                      text = "hide",
                      labels = c("cdm_name", "denominator_cohort_name"),
                      input_id = "incidence_gt_18_hide"
                    )
                  ),
                  position = "right"
                ),
                gt::gt_output("incidence_gt_18")
              )
            )
          ),
          bslib::nav_panel(
            title = "Plot incidence",
            bslib::card(
              full_screen = TRUE,
              bslib::card_header(
                bslib::popover(
                  shiny::icon("download"),
                  shiny::numericInput(
                    inputId = "incidence_ggplot2_19_download_width",
                    label = "Width",
                    value = 15
                  ),
                  shiny::numericInput(
                    inputId = "incidence_ggplot2_19_download_height",
                    label = "Height",
                    value = 10
                  ),
                  shinyWidgets::pickerInput(
                    inputId = "incidence_ggplot2_19_download_units",
                    label = "Units",
                    selected = "cm",
                    choices = c("px", "cm", "inch"),
                    multiple = FALSE
                  ),
                  shiny::numericInput(
                    inputId = "incidence_ggplot2_19_download_dpi",
                    label = "dpi",
                    value = 300
                  ),
                  shiny::downloadButton(outputId = "incidence_ggplot2_19_download", label = "Download")
                ),
                class = "text-end"
              ),
              bslib::layout_sidebar(
                sidebar = bslib::sidebar(
                  shinyWidgets::pickerInput(
                    inputId = "incidence_ggplot2_19_x",
                    label = "x",
                    selected = "incidence_start_date",
                    multiple = FALSE,
                    # choices = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "incidence_start_date", "incidence_end_date", "analysis_complete_database_intervals", "analysis_outcome_washout", "analysis_repeated_events", "denominator_age_group", "denominator_days_prior_observation", "denominator_end_date", "denominator_sex", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk", "variable_name", "variable_level", "estimate_name"),
                    choices = filterValues$incidence_tidy_columns,
                    options = list(`actions-box` = TRUE, size = 5, `selected-text-format` = "count > 3",container = "body")
                  ),
                  shiny::checkboxInput(
                    inputId = "incidence_ggplot2_19_ribbon",
                    label = "ribbon",
                    value = c(FALSE)
                  ),
                  shinyWidgets::pickerInput(
                    inputId = "incidence_ggplot2_19_facet",
                    label = "facet",
                    selected = NULL,
                    multiple = TRUE,
                    choices = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "incidence_start_date", "incidence_end_date", 
                                "analysis_complete_database_intervals", "analysis_outcome_washout", "analysis_repeated_events", 
                                "denominator_age_group", "denominator_days_prior_observation", "denominator_end_date", "denominator_sex", 
                                "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk", "variable_name", 
                                "variable_level", "estimate_name", filterValues$incidence_strata),
                    options = list(`actions-box` = TRUE, size = 5, `selected-text-format` = "count > 3",container = "body")
                  ),
                  shinyWidgets::pickerInput(
                    inputId = "incidence_ggplot2_19_colour",
                    label = "colour",
                    selected = c("outcome_cohort_name"),
                    multiple = TRUE,
                    choices = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "incidence_start_date", "incidence_end_date", 
                                "analysis_complete_database_intervals", "analysis_outcome_washout", "analysis_repeated_events", 
                                "denominator_age_group", "denominator_days_prior_observation", "denominator_end_date", "denominator_sex", 
                                "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk", "variable_name", 
                                "variable_level", "estimate_name", filterValues$incidence_strata),
                    options = list(`actions-box` = TRUE, size = 5, `selected-text-format` = "count > 3",container = "body")
                  ),
                  position = "right"
                ),
                shiny::plotOutput("incidence_ggplot2_19")
              )
            )
          )
        )
      )
    ),
    bslib::nav_panel(
      title = "Incidence attrition",
      icon = shiny::icon("layer-group"),
      bslib::layout_sidebar(
        sidebar = bslib::sidebar(
          bslib::accordion(
            bslib::accordion_panel(
              title = "Settings",
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_grouping_cdm_name",
                label = "Cdm name",
                choices = filterValues$incidence_attrition_grouping_cdm_name,
                selected = filterValues$incidence_attrition_grouping_cdm_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_cancer_group",
                label = "Cancer group",
                choices = filterValues$cancer_group_names,
                selected = filterValues$cancer_group_names[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_grouping_outcome_cohort_name",
                label = "Outcome cohort name",
                choices = filterValues$incidence_attrition_grouping_outcome_cohort_name,
                selected = filterValues$incidence_attrition_grouping_outcome_cohort_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_settings_denominator_sex",
                label = "Denominator sex",
                choices = filterValues$incidence_attrition_settings_denominator_sex,
                selected = filterValues$incidence_attrition_settings_denominator_sex[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "incidence_attrition_settings_denominator_age_group",
                label = "Denominator age group",
                choices = filterValues$incidence_attrition_settings_denominator_age_group,
                selected = filterValues$incidence_attrition_settings_denominator_age_group[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              )
            )
          )
        ),
        bslib::navset_tab(
          bslib::nav_panel(
            title = "Tidy",
            bslib::card(
              full_screen = TRUE,
              bslib::card_header(
                bslib::popover(
                  shiny::icon("download"),
                  shiny::downloadButton(outputId = "incidence_attrition_tidy_download", label = "Download csv")
                ),
                class = "text-end"
              ),
              bslib::layout_sidebar(
                sidebar = bslib::sidebar(
                  open = FALSE,
                  shinyWidgets::pickerInput(
                    inputId = "incidence_attrition_tidy_columns",
                    label = "Columns",
                    choices = filterValues$incidence_attrition_tidy_columns,
                    selected = NULL,
                    multiple = TRUE,
                    options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
                  ),
                  # shiny::radioButtons(
                  #   inputId = "incidence_attrition_tidy_pivot",
                  #   label = "Pivot estimates/variables",
                  #   choices = c("none", "estimates", "estimates and variables"),
                  #   selected = "estimates and variables"
                  # ),
                  position = "right"
                ),
                div(
                  style = "flex-grow: 1; overflow: auto;",
                  DT::dataTableOutput("incidence_attrition_tidy")
                )
                # DT::dataTableOutput("incidence_attrition_tidy")
              )
            )
          )
          # ,
          # bslib::nav_panel(
          #   title = "Diagram",
          #   bslib::card(
          #     full_screen = TRUE,
          #     bslib::card_header(
          #       bslib::popover(
          #         shiny::icon("download"),
          #         shiny::numericInput(
          #           inputId = "summarise_cohort_attrition_grViz_4_download_width",
          #           label = "Width (px)",
          #           value = 15
          #         ),
          #         shiny::numericInput(
          #           inputId = "summarise_cohort_attrition_grViz_4_download_height",
          #           label = "Height (px)",
          #           value = 10
          #         ),
          #         shiny::downloadButton(outputId = "summarise_cohort_attrition_grViz_4_download", label = "Download")
          #       ),
          #       class = "text-end"
          #     ),
          #     DiagrammeR::grVizOutput("summarise_cohort_attrition_grViz_4")
          #   )
          # )
        )
      )
    )
  ),
  ## PREVALENCE ###############################################
  bslib::nav_menu(
    title = "Prevalence",
    icon = shiny::icon("bath"),
    bslib::nav_panel(
      title = "Prevalence",
      icon = shiny::icon("chart-area"),
      bslib::layout_sidebar(
        sidebar = bslib::sidebar(
          bslib::accordion(
            bslib::accordion_panel(
              title = "Settings",
              shinyWidgets::pickerInput(
                inputId = "prevalence_grouping_cdm_name",
                label = "Cdm name",
                choices = filterValues$prevalence_grouping_cdm_name,
                selected = filterValues$prevalence_grouping_cdm_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_cancer_group",
                label = "Cancer group",
                choices = filterValues$cancer_group_names,
                selected = filterValues$cancer_group_names[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_grouping_outcome_cohort_name",
                label = "Outcome cohort name",
                choices = filterValues$prevalence_grouping_outcome_cohort_name,
                selected = filterValues$prevalence_grouping_outcome_cohort_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_grouping_prevalence_start_date",
                label = "Prevalence start date",
                choices = filterValues$prevalence_grouping_prevalence_start_date,
                selected = filterValues$prevalence_grouping_prevalence_start_date,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              )
              # shinyWidgets::pickerInput(
              #   inputId = "prevalence_grouping_prevalence_end_date",
              #   label = "Prevalence end date",
              #   choices = filterValues$prevalence_grouping_prevalence_end_date,
              #   selected = filterValues$prevalence_grouping_prevalence_end_date,
              #   multiple = TRUE,
              #   options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              # ),
            ),
            bslib::accordion_panel(
              title = "Strata",
              shinyWidgets::pickerInput(
                inputId = "prevalence_settings_denominator_sex",
                label = "Denominator sex",
                choices = filterValues$prevalence_settings_denominator_sex,
                selected = filterValues$prevalence_settings_denominator_sex[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_settings_denominator_age_group",
                label = "Denominator age group",
                choices = filterValues$prevalence_settings_denominator_age_group,
                selected = filterValues$prevalence_settings_denominator_age_group[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              )
            ),
            bslib::accordion_panel(
              title = "Estimates",
              # shinyWidgets::pickerInput(
              #   inputId = "prevalence_variable_name",
              #   label = "Variable name",
              #   choices = filterValues$prevalence_variable_name,
              #   selected = filterValues$prevalence_variable_name,
              #   multiple = TRUE,
              #   options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              # ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_estimate_name",
                label = "Estimate name",
                choices = filterValues$prevalence_estimate_name,
                selected = filterValues$prevalence_estimate_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              )
            )
          )
        ),
        bslib::navset_tab(
          bslib::nav_panel(
            title = "Tidy",
            bslib::card(
              full_screen = TRUE,
              bslib::card_header(
                bslib::popover(
                  shiny::icon("download"),
                  shiny::downloadButton(outputId = "prevalence_tidy_download", label = "Download csv")
                ),
                class = "text-end"
              ),
              bslib::layout_sidebar(
                sidebar = bslib::sidebar(
                  open = FALSE,
                  shinyWidgets::pickerInput(
                    inputId = "prevalence_tidy_columns",
                    label = "Columns",
                    choices = filterValues$prevalence_tidy_columns,
                    # selected = filterValues$prevalence_tidy_columns,
                    selected = c("denominator_cohort_name", "outcome_cohort_name", 
                                 "prevalence_start_date","prevalence_end_date"),
                    multiple = TRUE,
                    options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
                  ),
                  shiny::radioButtons(
                    inputId = "prevalence_tidy_pivot",
                    label = "Pivot estimates/variables",
                    choices = c("none", "estimates", "estimates and variables"),
                    selected = "estimates and variables"
                  ),
                  position = "right"
                ),
                div(
                  style = "flex-grow: 1; overflow: auto;",
                  DT::dataTableOutput("prevalence_tidy")
                )
                # DT::dataTableOutput("prevalence_tidy")
              )
            )
          ),
          bslib::nav_panel(
            title = "Table prevalence",
            bslib::card(
              full_screen = TRUE,
              bslib::card_header(
                bslib::popover(
                  shiny::icon("download"),
                  shinyWidgets::pickerInput(
                    inputId = "prevalence_gt_18_download_type",
                    label = "File type",
                    selected = "docx",
                    choices = c("docx", "png", "pdf", "html"),
                    multiple = FALSE
                  ),
                  shiny::downloadButton(outputId = "prevalence_gt_18_download", label = "Download")
                ),
                class = "text-end"
              ),
              bslib::layout_sidebar(
                sidebar = bslib::sidebar(
                  open=FALSE,
                  sortable::bucket_list(
                    header = NULL,
                    sortable::add_rank_list(
                      text = "none",
                      # labels = c("prevalence_start_date", "prevalence_end_date", "analysis_complete_database_intervals", "analysis_outcome_washout", "analysis_repeated_events", "denominator_age_group", "denominator_days_prior_observation", "denominator_end_date", "denominator_sex", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk", "variable_name", "variable_level")
                      labels = setdiff (filterValues$prevalence_tidy_columns, c("estimate_name","cdm_name", "outcome_cohort_name", "denominator_cohort_name")  ),
                      input_id = "prevalence_gt_18_none"
                    ),
                    sortable::add_rank_list(
                      text = "header",
                      labels = "estimate_name",
                      input_id = "prevalence_gt_18_header"
                    ),
                    sortable::add_rank_list(
                      text = "groupColumn",
                      labels = c("cdm_name", "outcome_cohort_name"),
                      input_id = "prevalence_gt_18_groupColumn"
                    ),
                    sortable::add_rank_list(
                      text = "hide",
                      labels = "denominator_cohort_name",
                      input_id = "prevalence_gt_18_hide"
                    )
                  ),
                  position = "right"
                ),
                gt::gt_output("prevalence_gt_18")
              )
            )
          ),
          bslib::nav_panel(
            title = "Plot prevalence",
            bslib::card(
              full_screen = TRUE,
              bslib::card_header(
                bslib::popover(
                  shiny::icon("download"),
                  shiny::numericInput(
                    inputId = "prevalence_ggplot2_19_download_width",
                    label = "Width",
                    value = 15
                  ),
                  shiny::numericInput(
                    inputId = "prevalence_ggplot2_19_download_height",
                    label = "Height",
                    value = 10
                  ),
                  shinyWidgets::pickerInput(
                    inputId = "prevalence_ggplot2_19_download_units",
                    label = "Units",
                    selected = "cm",
                    choices = c("px", "cm", "inch"),
                    multiple = FALSE
                  ),
                  shiny::numericInput(
                    inputId = "prevalence_ggplot2_19_download_dpi",
                    label = "dpi",
                    value = 300
                  ),
                  shiny::downloadButton(outputId = "prevalence_ggplot2_19_download", label = "Download")
                ),
                class = "text-end"
              ),
              bslib::layout_sidebar(
                sidebar = bslib::sidebar(
                  shinyWidgets::pickerInput(
                    inputId = "prevalence_ggplot2_19_x",
                    label = "x",
                    selected = "prevalence_start_date",
                    multiple = FALSE,
                    choices = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "prevalence_start_date", "prevalence_end_date", "analysis_complete_database_intervals", "analysis_outcome_washout", "analysis_repeated_events", "denominator_age_group", "denominator_days_prior_observation", "denominator_end_date", "denominator_sex", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk", "variable_name", "variable_level", "estimate_name"),
                    options = list(`actions-box` = TRUE, size = 5, `selected-text-format` = "count > 3",container = "body")
                  ),
                  shiny::checkboxInput(
                    inputId = "prevalence_ggplot2_19_ribbon",
                    label = "ribbon",
                    value = c(FALSE)
                  ),
                  shinyWidgets::pickerInput(
                    inputId = "prevalence_ggplot2_19_facet",
                    label = "facet",
                    selected = NULL,
                    multiple = TRUE,
                    choices = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "prevalence_start_date", "prevalence_end_date", "analysis_complete_database_intervals", "analysis_outcome_washout", "analysis_repeated_events", "denominator_age_group", "denominator_days_prior_observation", "denominator_end_date", "denominator_sex", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk", "variable_name", "variable_level", "estimate_name"),
                    options = list(`actions-box` = TRUE, size = 5, `selected-text-format` = "count > 3",container = "body")
                  ),
                  shinyWidgets::pickerInput(
                    inputId = "prevalence_ggplot2_19_colour",
                    label = "colour",
                    selected = c("outcome_cohort_name"),
                    multiple = TRUE,
                    choices = c("cdm_name", "denominator_cohort_name", "outcome_cohort_name", "prevalence_start_date", "prevalence_end_date", "analysis_complete_database_intervals", "analysis_outcome_washout", "analysis_repeated_events", "denominator_age_group", "denominator_days_prior_observation", "denominator_end_date", "denominator_sex", "denominator_start_date", "denominator_target_cohort_name", "denominator_time_at_risk", "variable_name", "variable_level", "estimate_name"),
                    options = list(`actions-box` = TRUE, size = 5, `selected-text-format` = "count > 3",container = "body")
                  ),
                  position = "right"
                ),
                shiny::plotOutput("prevalence_ggplot2_19")
              )
            )
          )
        )
      )
    ),
    bslib::nav_panel(
      title = "Prevalence attrition",
      icon = shiny::icon("layer-group"),
      bslib::layout_sidebar(
        sidebar = bslib::sidebar(
          bslib::accordion(
            bslib::accordion_panel(
              title = "Settings",
              shinyWidgets::pickerInput(
                inputId = "prevalence_attrition_grouping_cdm_name",
                label = "Cdm name",
                choices = filterValues$prevalence_attrition_grouping_cdm_name,
                selected = filterValues$prevalence_attrition_grouping_cdm_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_attrition_cancer_group",
                label = "Cancer group",
                choices = filterValues$cancer_group_names,
                selected = filterValues$cancer_group_names[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_attrition_grouping_outcome_cohort_name",
                label = "Outcome cohort name",
                choices = filterValues$prevalence_attrition_grouping_outcome_cohort_name,
                selected = filterValues$prevalence_attrition_grouping_outcome_cohort_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_attrition_settings_denominator_sex",
                label = "Denominator sex",
                choices = filterValues$prevalence_attrition_settings_denominator_sex,
                selected = filterValues$prevalence_attrition_settings_denominator_sex[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "prevalence_attrition_settings_denominator_age_group",
                label = "Denominator age group",
                choices = filterValues$prevalence_attrition_settings_denominator_age_group,
                selected = filterValues$prevalence_attrition_settings_denominator_age_group[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              )
            )
          )
        ),
        bslib::navset_tab(
          bslib::nav_panel(
            title = "Tidy",
            bslib::card(
              full_screen = TRUE,
              bslib::card_header(
                bslib::popover(
                  shiny::icon("download"),
                  shiny::downloadButton(outputId = "prevalence_attrition_tidy_download", label = "Download csv")
                ),
                class = "text-end"
              ),
              bslib::layout_sidebar(
                sidebar = bslib::sidebar(
                  open = FALSE,
                  shinyWidgets::pickerInput(
                    inputId = "prevalence_attrition_tidy_columns",
                    label = "Columns",
                    choices = filterValues$prevalence_attrition_tidy_columns,
                    selected = NULL,
                    multiple = TRUE,
                    options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
                  ),
                  position = "right"
                ),
                div(
                  style = "flex-grow: 1; overflow: auto;",
                  DT::dataTableOutput("prevalence_attrition_tidy")
                )
                # DT::dataTableOutput("prevalence_attrition_tidy")
              )
            )
          )
        )
      )
    )
  ),
  ## SURVIVAL ###############################################
  bslib::nav_menu(
    title = "Survival",
    icon = shiny::icon("clock"),
    bslib::nav_panel(
      title = "Survival summary",
      icon = shiny::icon("chart-bar"),
      bslib::layout_sidebar(
        sidebar = bslib::sidebar(
          bslib::accordion(
            bslib::accordion_panel(
              title = "Settings",
              shinyWidgets::pickerInput(
                inputId = "survival_summary_grouping_cdm_name",
                label = "Cdm name",
                choices = filterValues$survival_summary_grouping_cdm_name,
                selected = filterValues$survival_summary_grouping_cdm_name[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_summary_cancer_group",
                label = "Cancer group",
                choices = filterValues$cancer_group_names,
                selected = filterValues$cancer_group_names[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_summary_grouping_target_cohort",
                label = "Target cohort",
                choices = filterValues$survival_summary_grouping_target_cohort,
                selected = filterValues$survival_summary_grouping_target_cohort,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_summary_grouping_study_period",
                label = "Study Period",
                choices = filterValues$survival_summary_grouping_study_period,
                selected = filterValues$survival_summary_grouping_study_period[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_summary_grouping_year",
                label = "Survival year",
                choices = filterValues$survival_summary_grouping_year,
                selected = filterValues$survival_summary_grouping_year[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              )
            ),
            bslib::accordion_panel(
              title = "Strata",
              shinyWidgets::pickerInput(
                inputId = "survival_summary_grouping_age_gr",
                label = "Age group",
                choices = filterValues$survival_summary_grouping_age_gr,
                selected = filterValues$survival_summary_grouping_age_gr[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_summary_grouping_sex",
                label = "Sex",
                choices = filterValues$survival_summary_grouping_sex,
                selected = filterValues$survival_summary_grouping_sex[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_summary_strata",
                label = "Strata variables",
                choices = filterValues$survival_strata,
                selected = NULL,  # por defecto ninguna seleccionada = todo overall
                multiple = TRUE,
                options = list(
                  `actions-box` = TRUE,
                  size = 7,
                  # `live-search` = TRUE,
                  container = "body"
                )
              )
            ),
            bslib::accordion_panel(
              title = "Estimates",
              shinyWidgets::pickerInput(
                inputId = "survival_summary_estimate_name",
                label = "Estimate name",
                choices = filterValues$survival_summary_estimate_name,
                selected = filterValues$survival_summary_estimate_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              )
            )
          )
        ),
        bslib::navset_tab(
          bslib::nav_panel(
            title = "Tidy",
            bslib::card(
              full_screen = TRUE,
              bslib::card_header(
                bslib::popover(
                  shiny::icon("download"),
                  shiny::downloadButton(outputId = "survival_tidy_download", label = "Download csv")
                ),
                class = "text-end"
              ),
              bslib::layout_sidebar(
                sidebar = bslib::sidebar(
                  open = FALSE,
                  shinyWidgets::pickerInput(
                    inputId = "survival_summary_tidy_columns",
                    label = "Columns",
                    choices = filterValues$survival_summary_tidy_columns,
                    selected = c("cdm_name", "target_cohort", "age_gr", "sex", "study_period" , "year", "estimate_name"),
                    multiple = TRUE,
                    options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
                  ),
                  shiny::radioButtons(
                    inputId = "survival_summary_tidy_pivot",
                    label = "Pivot estimates/variables",
                    choices = c("none", "estimates", "estimates and variables"),
                    selected = "estimates"
                  ),
                  position = "right"
                ),
                div(
                  style = "flex-grow: 1; overflow: auto;",
                  DT::dataTableOutput("survival_tidy")
                )
              )
            )
          ),
          bslib::nav_panel(
            title = "Table",
            bslib::card(
              full_screen = TRUE,
              bslib::card_header(
                bslib::popover(
                  shiny::icon("download"),
                  shinyWidgets::pickerInput(
                    inputId = "survival_gt_18_download_type",
                    label = "File type",
                    selected = "docx",
                    choices = c("docx", "png", "pdf", "html"),
                    multiple = FALSE
                  ),
                  shiny::downloadButton(outputId = "survival_gt_18_download", label = "Download")
                ),
                class = "text-end"
              ),
              bslib::layout_sidebar(
                sidebar = bslib::sidebar(
                  open = FALSE,
                  sortable::bucket_list(
                    header = NULL,
                    sortable::add_rank_list(
                      text = "none",
                      labels = c("age_gr", "sex", "study_period", "year", 
                                 filterValues$survival_strata  ),
                      input_id = "survival_summary_gt_18_none"
                    ),
                    sortable::add_rank_list(
                      text = "header",
                      labels = "estimate_name",
                      input_id = "survival_summary_gt_18_header"
                    ),
                    sortable::add_rank_list(
                      text = "groupColumn",
                      labels = c("target_cohort"),
                      input_id = "survival_summary_gt_18_groupColumn"
                    ),
                    sortable::add_rank_list(
                      text = "hideColumn",
                      labels = c("cdm_name"),
                      input_id = "survival_summary_gt_18_hideColumn"
                    )
                  ),
                  position = "right"
                ),
                gt::gt_output("survival_summary_gt_18")
              )
            )
          )
        )
      )
    ),
    bslib::nav_panel(
      title = "Risk table",
      icon = shiny::icon("clock"),
      bslib::layout_sidebar(
        sidebar = bslib::sidebar(
          bslib::accordion(
            bslib::accordion_panel(
              title = "Settings",
              shinyWidgets::pickerInput(
                inputId = "survival_events_grouping_cdm_name",
                label = "Cdm name",
                choices = filterValues$survival_events_grouping_cdm_name,
                selected = filterValues$survival_events_grouping_cdm_name[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_events_cancer_group",
                label = "Cancer group",
                choices = filterValues$cancer_group_names,
                selected = filterValues$cancer_group_names[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_events_grouping_target_cohort",
                label = "Target cohort",
                choices = filterValues$survival_events_grouping_target_cohort,
                selected = filterValues$survival_events_grouping_target_cohort,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_events_grouping_study_period",
                label = "Study Period",
                choices = filterValues$survival_events_grouping_study_period,
                selected = filterValues$survival_events_grouping_study_period[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_events_grouping_year",
                label = "Survival year",
                choices = filterValues$survival_events_grouping_year,
                selected = filterValues$survival_events_grouping_year[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              )
            ),
            bslib::accordion_panel(
              title = "Strata",
              shinyWidgets::pickerInput(
                inputId = "survival_events_grouping_age_gr",
                label = "Age group",
                choices = filterValues$survival_events_grouping_age_gr,
                selected = filterValues$survival_events_grouping_age_gr[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_events_grouping_sex",
                label = "Sex",
                choices = filterValues$survival_events_grouping_sex,
                selected = filterValues$survival_events_grouping_sex[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_events_strata",
                label = "Strata variables",
                choices = filterValues$survival_strata,
                selected = NULL,  # por defecto ninguna seleccionada = todo overall
                multiple = TRUE,
                options = list(
                  `actions-box` = TRUE,
                  size = 7,
                  # `live-search` = TRUE,
                  container = "body"
                )
              )
            )
          )
        ),
        bslib::navset_tab(
          bslib::nav_panel(
            title = "Tidy",
            bslib::card(
              full_screen = TRUE,
              bslib::card_header(
                bslib::popover(
                  shiny::icon("download"),
                  shiny::downloadButton(outputId = "survival_events_tidy_download", label = "Download csv")
                ),
                class = "text-end"
              ),
              bslib::layout_sidebar(
                sidebar = bslib::sidebar(
                  open = FALSE,
                  shinyWidgets::pickerInput(
                    inputId = "survival_events_tidy_columns",
                    label = "Columns",
                    choices = filterValues$survival_events_tidy_columns,
                    selected = c("cdm_name", "target_cohort", "age_gr", "sex", "study_period" , "year", "estimate_name", "time"),
                    multiple = TRUE,
                    options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
                  ),
                  shiny::radioButtons(
                    inputId = "survival_events_tidy_pivot",
                    label = "Pivot estimates/variables",
                    choices = c("none", "estimates", "estimates and variables"),
                    selected = "estimates"
                  ),
                  position = "right"
                ),
                div(
                  style = "flex-grow: 1; overflow: auto;",
                  DT::dataTableOutput("survival_risk_tidy")
                )
              )
            )
        ),
        bslib::nav_panel(
          title = "Table",
          bslib::card(
            full_screen = TRUE,
            bslib::card_header(
              class = "text-end"
            ),
            bslib::layout_sidebar(
              sidebar = bslib::sidebar(
                open = FALSE,
                sortable::bucket_list(
                  header = NULL,
                  sortable::add_rank_list(
                    text = "none",
                    labels = c("cdm_name", "age_gr", "sex", "study_period", "year", 
                               "target_cohort", filterValues$survival_strata  ),
                    input_id = "survival_events_gt_18_none"
                  ),
                  sortable::add_rank_list(
                    text = "header",
                    labels = "estimate_name",
                    input_id = "survival_events_gt_18_header"
                  ),
                  sortable::add_rank_list(
                    text = "groupColumn",
                    labels = NULL,
                    input_id = "survival_events_gt_18_groupColumn"
                  )
                ),
                position = "right"
              ),
              gt::gt_output("survival_events_gt_18")
              )
            )
          )
        )
      )
    ),
    bslib::nav_panel(
      title = "Survival probability",
      icon = shiny::icon("chart-line"),
      bslib::layout_sidebar(
        sidebar = bslib::sidebar(
          bslib::accordion(
            bslib::accordion_panel(
              title = "Settings",
              shinyWidgets::pickerInput(
                inputId = "survival_probability_grouping_cdm_name",
                label = "Cdm name",
                choices = filterValues$survival_probability_grouping_cdm_name,
                selected = filterValues$survival_probability_grouping_cdm_name[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_probability_cancer_group",
                label = "Cancer group",
                choices = filterValues$cancer_group_names,
                selected = filterValues$cancer_group_names[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_probability_grouping_target_cohort",
                label = "Target cohort",
                choices = filterValues$survival_probability_grouping_target_cohort,
                selected = filterValues$survival_probability_grouping_target_cohort,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_probability_grouping_study_period",
                label = "Study Period",
                choices = filterValues$survival_probability_grouping_study_period,
                selected = filterValues$survival_probability_grouping_study_period[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_probability_grouping_year",
                label = "Survival year",
                choices = filterValues$survival_probability_grouping_year,
                selected = filterValues$survival_probability_grouping_year[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              )
            ),
            bslib::accordion_panel(
              title = "Strata",
              shinyWidgets::pickerInput(
                inputId = "survival_probability_grouping_age_gr",
                label = "Age group",
                choices = filterValues$survival_probability_grouping_age_gr,
                selected = filterValues$survival_probability_grouping_age_gr[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_probability_grouping_sex",
                label = "Sex",
                choices = filterValues$survival_probability_grouping_sex,
                selected = filterValues$survival_probability_grouping_sex[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_probability_strata",
                label = "Strata variables",
                choices = filterValues$survival_strata,
                selected = NULL,  # por defecto ninguna seleccionada = todo overall
                multiple = TRUE,
                options = list(
                  `actions-box` = TRUE,
                  size = 7,
                  # `live-search` = TRUE,
                  container = "body"
                )
              )
            )
          )
        ),
        bslib::navset_tab(
          bslib::nav_panel(
            title = "Plot survival",
            bslib::card(
              full_screen = TRUE,
              bslib::card_header(
                bslib::popover(
                  shiny::icon("download"),
                  shiny::numericInput(
                    inputId = "survival_ggplot2_19_download_width",
                    label = "Width",
                    value = 15
                  ),
                  shiny::numericInput(
                    inputId = "survival_ggplot2_19_download_height",
                    label = "Height",
                    value = 10
                  ),
                  shinyWidgets::pickerInput(
                    inputId = "survival_ggplot2_19_download_units",
                    label = "Units",
                    selected = "cm",
                    choices = c("px", "cm", "inch"),
                    multiple = FALSE
                  ),
                  shiny::numericInput(
                    inputId = "survival_ggplot2_19_download_dpi",
                    label = "dpi",
                    value = 300
                  ),
                  shiny::downloadButton(outputId = "survival_ggplot2_19_download", label = "Download")
                ),
                class = "text-end"
              ),
              bslib::layout_sidebar(
                sidebar = bslib::sidebar(
                  shiny::checkboxInput(
                    inputId = "survival_ggplot2_19_ribbon",
                    label = "ribbon",
                    value = c(TRUE)
                  ),
                  shinyWidgets::pickerInput(
                    inputId = "survival_ggplot2_19_facet",
                    label = "facet",
                    selected = NULL,
                    multiple = TRUE,
                    choices = c("cdm_name", "target_cohort",  "study_period", "age_gr", "sex", "year", filterValues$survival_strata),
                    options = list(`actions-box` = TRUE, size = 5, `selected-text-format` = "count > 3",container = "body")
                  ),
                  shinyWidgets::pickerInput(
                    inputId = "survival_ggplot2_19_colour",
                    label = "colour",
                    selected = c("target_cohort"),
                    multiple = TRUE,
                    choices = c("cdm_name", "target_cohort",  "study_period", "age_gr", "sex", "year", filterValues$survival_strata),
                    options = list(`actions-box` = TRUE, size = 5, `selected-text-format` = "count > 3",container = "body")
                  ),
                  position = "right"
                ),
                shiny::plotOutput("survival_ggplot2_19")
              )
            )
          ),
          bslib::nav_panel(
            title = "Tidy",
            bslib::card(
              full_screen = TRUE,
              bslib::card_header(
                bslib::popover(
                  shiny::icon("download"),
                  shiny::downloadButton(outputId = "survival_probability_tidy_download", label = "Download csv")
                ),
                class = "text-end"
              ),
              bslib::layout_sidebar(
                sidebar = bslib::sidebar(
                  open = FALSE,
                  shinyWidgets::pickerInput(
                    inputId = "survival_probability_tidy_columns",
                    label = "Columns",
                    choices = filterValues$survival_probability_tidy_columns,
                    selected = c("cdm_name", "target_cohort", "age_gr", "sex", "study_period" , "year", "estimate_name", "time"),
                    multiple = TRUE,
                    options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
                  ),
                  shiny::radioButtons(
                    inputId = "survival_probability_tidy_pivot",
                    label = "Pivot estimates/variables",
                    choices = c("none", "estimates", "estimates and variables"),
                    selected = "estimates"
                  ),
                  position = "right"
                ),
                div(
                  style = "flex-grow: 1; overflow: auto;",
                  DT::dataTableOutput("survival_probability_tidy")
                )
              )
            )
          )
        )
      )
    ),
    bslib::nav_panel(
      title = "Survival attrition",
      icon = shiny::icon("layer-group"),
      bslib::layout_sidebar(
        sidebar = bslib::sidebar(
          bslib::accordion(
            bslib::accordion_panel(
              title = "Settings",
              shinyWidgets::pickerInput(
                inputId = "survival_attrition_grouping_cdm_name",
                label = "Cdm name",
                choices = filterValues$survival_attrition_grouping_cdm_name,
                selected = filterValues$survival_attrition_grouping_cdm_name,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_attrition_cancer_group",
                label = "Cancer group",
                choices = filterValues$cancer_group_names,
                selected = filterValues$cancer_group_names[1],
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              ),
              shinyWidgets::pickerInput(
                inputId = "survival_attrition_grouping_target_cohort",
                label = "Target cohort",
                choices = filterValues$survival_attrition_grouping_target_cohort,
                selected = filterValues$survival_attrition_grouping_target_cohort,
                multiple = TRUE,
                options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
              )
            )
          )
        ),
        bslib::navset_tab(
          bslib::nav_panel(
            title = "Tidy",
            bslib::card(
              full_screen = TRUE,
              bslib::card_header(
                bslib::popover(
                  shiny::icon("download"),
                  shiny::downloadButton(outputId = "survival_attrition_tidy_download", label = "Download csv")
                ),
                class = "text-end"
              ),
              bslib::layout_sidebar(
                sidebar = bslib::sidebar(
                  open = FALSE,
                  shinyWidgets::pickerInput(
                    inputId = "survival_attrition_tidy_columns",
                    label = "Columns",
                    choices = filterValues$survival_attrition_tidy_columns,
                    selected = NULL,
                    multiple = TRUE,
                    options = list(`actions-box` = TRUE, size = 7, `selected-text-format` = "count > 3",container = "body")
                  ),
                  # shiny::radioButtons(
                  #   inputId = "survival_attrition_tidy_pivot",
                  #   label = "Pivot estimates/variables",
                  #   choices = c("none", "estimates", "estimates and variables"),
                  #   selected = "estimates and variables"
                  # ),
                  position = "right"
                ),
                div(
                  style = "flex-grow: 1; overflow: auto;",
                  DT::dataTableOutput("survival_attrition_tidy")
                )
                # DT::dataTableOutput("survival_attrition_tidy")
              )
            )
          )
        )
      )
    )
  )
  # ,
  # bslib::nav_spacer(),
  # bslib::nav_item(
  #   bslib::popover(
  #     shiny::icon("download"),
  #     shiny::downloadButton(
  #       outputId = "download_raw",
  #       label = "Download raw data",
  #       icon = shiny::icon("download")
  #     )
  #   )
  # ),
  # bslib::nav_item(
  #   bslib::popover(
  #     shiny::icon("circle-info"),
  #     shiny::tags$img(
  #       src = "hds_logo.svg",
  #       class = "logo-img",
  #       alt = "Logo",
  #       height = "auto",
  #       width = "30%",
  #       style = "float:right"
  #     ),
  #     "This shiny app was generated with ",
  #     shiny::a(
  #       "OmopViewer",
  #       href = "https://github.com/OHDSI/OmopViewer",
  #       target = "_blank"
  #     ),
  #     shiny::strong("v0.2.0")
  #   )
  # ),
  # bslib::nav_item(bslib::input_dark_mode(id = "dark_mode", mode = "light"))
)
